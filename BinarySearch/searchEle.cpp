#include <bits/stdc++.h>
using namespace std;

int binary_search(int arr, int low, int high, int target){
    while(low<=higj){
    int mid = (low + high)/2;
    
    else if(mid == target) return mid;
    else if (mid > target){
        high = mid -1;
    }
    else{
        low = mid+1;
    }
}
return -1
}